.DS_Store

node_modules
.vscode
.zed
.DS_Store
.env
.env.development
.env.production
.cache
.vite
.secrets
dist
fly.toml
fly.dev.toml
fly.prod.toml

.react-router/
build/

workdocs/

# Caddy data (certificates, etc)
caddy_data/

.playwright-mcp
.backups

# Claude Code: worktrees, local settings and transcripts stay local; the
# launch config (preview server) and project hooks are shared.
.claude/*
!.claude/launch.json
!.claude/settings.json

supabase
